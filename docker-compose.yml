version: '3.8'
services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "5050:5050"
    restart: unless-stopped
    # For production, mount a more robust data volume or connect to a managed DB instead of lowdb file
    volumes:
      - ./:/usr/src/app:ro
